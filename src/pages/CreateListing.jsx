import { ListigForm } from "../components/auth";

const Login = () => {
    return (
        <div className="pt-20 px-[3%] md:px-[6%]">
            <ListigForm />
        </div>
    );
};

export default Login;
